/// Detects active Claude Code sessions by scanning running processes.
/// Stub implementation -- full functionality planned for future phases.
pub struct SessionDetector {
    _placeholder: (),
}

impl SessionDetector {
    pub fn new() -> Self {
        Self { _placeholder: () }
    }
}

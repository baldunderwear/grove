---
phase: 09-terminal-foundation-conpty-spike
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - src-ui/src/pages/Dashboard.tsx
  - src-ui/src/components/BranchTable.tsx
  - src-ui/src/components/ui/resizable.tsx
autonomous: false
requirements:
  - TERM-01
  - TERM-04
  - TERM-05
  - NFR-05
  - NFR-06
  - NFR-07
  - NFR-08

must_haves:
  truths:
    - "Clicking Launch on a worktree opens an embedded terminal pane instead of external window"
    - "Dashboard splits into branch list + terminal with a draggable resizer"
    - "Terminal renders ANSI colors and cursor movement correctly"
    - "Terminal resizes fluidly when dragging the pane divider"
    - "No visible cmd.exe window appears during terminal launch"
    - "Terminal works for NAS-hosted worktrees (UNC path resolution)"
  artifacts:
    - path: "src-ui/src/pages/Dashboard.tsx"
      provides: "Split-pane layout with conditional terminal panel"
      contains: "ResizablePanelGroup"
    - path: "src-ui/src/components/BranchTable.tsx"
      provides: "Launch button opens embedded terminal"
      contains: "useTerminalStore"
    - path: "src-ui/src/components/ui/resizable.tsx"
      provides: "shadcn resizable component"
      contains: "ResizablePanel"
  key_links:
    - from: "src-ui/src/components/BranchTable.tsx"
      to: "src-ui/src/stores/terminal-store.ts"
      via: "Launch button calls openTerminal() in store"
      pattern: "openTerminal"
    - from: "src-ui/src/pages/Dashboard.tsx"
      to: "src-ui/src/components/terminal/TerminalPanel.tsx"
      via: "Renders TerminalPanel when activeTerminalId is set"
      pattern: "TerminalPanel"
    - from: "src-ui/src/pages/Dashboard.tsx"
      to: "src-ui/src/components/ui/resizable.tsx"
      via: "ResizablePanelGroup wraps branch table + terminal"
      pattern: "ResizablePanelGroup"
---

<objective>
Wire the Rust backend (Plan 01) and frontend components (Plan 02) into the Dashboard: add shadcn Resizable split-pane, connect BranchTable Launch button to embedded terminal, and validate the full ConPTY integration end-to-end.

Purpose: This is the integration plan that delivers the actual user-facing feature. The ConPTY spike is validated here -- if this works, the terminal foundation is solid.
Output: Working embedded terminal in Grove that launches Claude Code with full ANSI rendering, resizable panes, no CMD flash, and NAS support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-terminal-foundation-conpty-spike/09-RESEARCH.md
@.planning/phases/09-terminal-foundation-conpty-spike/09-01-SUMMARY.md
@.planning/phases/09-terminal-foundation-conpty-spike/09-02-SUMMARY.md

@src-ui/src/pages/Dashboard.tsx
@src-ui/src/components/BranchTable.tsx
@src-ui/src/stores/terminal-store.ts
@src-ui/src/components/terminal/TerminalPanel.tsx

<interfaces>
<!-- From Plan 01 (Rust backend): -->
```rust
// Tauri commands available:
terminal_spawn(working_dir: String, cols: u16, rows: u16, on_event: Channel<TerminalEvent>) -> String
terminal_write(terminal_id: String, data: String) -> ()
terminal_resize(terminal_id: String, cols: u16, rows: u16) -> ()
terminal_kill(terminal_id: String) -> ()
```

<!-- From Plan 02 (Frontend components): -->
```typescript
// Terminal store
import { useTerminalStore } from '@/stores/terminal-store';
// State: activeTerminalId, activeWorktreePath, activeBranchName, isConnected
// Actions: openTerminal(id, path, branch), closeTerminal(), setConnected(bool)

// Terminal panel
import { TerminalPanel } from '@/components/terminal/TerminalPanel';
// Props: terminalId: string, worktreePath: string, branchName: string

// shadcn Resizable (to be added)
import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from '@/components/ui/resizable';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Resizable component and wire Dashboard split-pane with BranchTable launch integration</name>
  <read_first>
    - src-ui/src/pages/Dashboard.tsx (full file -- understand current layout)
    - src-ui/src/components/BranchTable.tsx (full file -- find Launch button handler)
    - src-ui/src/stores/terminal-store.ts (from Plan 02)
    - src-ui/src/components/terminal/TerminalPanel.tsx (from Plan 02)
  </read_first>
  <files>
    src-ui/src/components/ui/resizable.tsx,
    src-ui/src/pages/Dashboard.tsx,
    src-ui/src/components/BranchTable.tsx
  </files>
  <action>
    1. Add shadcn Resizable component. Since NAS prevents npx shadcn directly, manually create the component:
       - Create `src-ui/src/components/ui/resizable.tsx` following shadcn pattern:
         - Install react-resizable-panels: `cd Z:/data/development/grove/src-ui && node ../scripts/with-modules.mjs npm install react-resizable-panels`
         - Create the shadcn Resizable wrapper component exporting `ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle`
         - Use the standard shadcn implementation (Panel = PanelGroup with className, Handle = PanelResizeHandle with GripVertical icon)

    2. Update `src-ui/src/pages/Dashboard.tsx` (per locked decision: split view, dashboard left, terminal right):
       - Import `useTerminalStore`, `TerminalPanel`, and Resizable components
       - Read `activeTerminalId`, `activeWorktreePath`, `activeBranchName` from terminal store
       - When `activeTerminalId` is set:
         - Wrap existing content in `<ResizablePanelGroup direction="horizontal" className="flex-1">`
         - Left panel: `<ResizablePanel defaultSize={50} minSize={25}>` containing existing branch table/dashboard content
         - Handle: `<ResizableHandle withHandle />`
         - Right panel: `<ResizablePanel defaultSize={50} minSize={30}>` containing `<TerminalPanel terminalId={activeTerminalId} worktreePath={activeWorktreePath} branchName={activeBranchName} />`
       - When `activeTerminalId` is null: render existing layout unchanged (no split)
       - IMPORTANT: The ResizablePanelGroup must fill available height. Use flex layout: parent `flex flex-col h-full`, panel group `flex-1`

    3. Update `src-ui/src/components/BranchTable.tsx`:
       - Import `useTerminalStore` from terminal-store
       - Find the existing "Launch" button handler (currently calls `launchSession` from session-store which spawns external window)
       - Change behavior: instead of calling `launchSession()`, call terminal store's `openTerminal()` with a placeholder ID
       - The actual PTY spawn happens in TerminalPanel on mount (it calls `terminal_spawn` via invoke)
       - So BranchTable just needs to set the terminal store state: `openTerminal('pending', worktreePath, branchName)`
       - TerminalPanel reads worktreePath from store and spawns the PTY, then updates the store with the real terminal ID
       - Keep the old `launchSession` code but behind a conditional or remove it (the embedded terminal replaces external launch per locked decision)
       - If a terminal is already open for a different worktree, close it first (single terminal for this phase per locked decision)

    4. Verify typecheck + lint pass.
  </action>
  <verify>
    <automated>cd Z:/data/development/grove/src-ui && node ../scripts/with-modules.mjs npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "ResizablePanelGroup" src-ui/src/pages/Dashboard.tsx returns match
    - grep "ResizablePanel" src-ui/src/pages/Dashboard.tsx returns match
    - grep "TerminalPanel" src-ui/src/pages/Dashboard.tsx returns match
    - grep "useTerminalStore" src-ui/src/pages/Dashboard.tsx returns match
    - grep "useTerminalStore" src-ui/src/components/BranchTable.tsx returns match
    - grep "openTerminal" src-ui/src/components/BranchTable.tsx returns match
    - file src-ui/src/components/ui/resizable.tsx exists
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Dashboard has conditional split-pane layout, BranchTable Launch button opens embedded terminal, shadcn Resizable wired. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: ConPTY Spike Validation -- Full End-to-End Test</name>
  <files>no files modified -- validation only</files>
  <action>
    Run `cargo tauri dev` to launch the app in dev mode. Then run `cargo tauri build` for release build validation. This task is a human-verify checkpoint -- the executor presents the verification steps and waits for user approval.
  </action>
  <verify>
    <automated>cd Z:/data/development/grove && cargo check 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms: embedded terminal launches Claude Code, ANSI renders, pane resizes, no CMD flash in release build, NAS paths work.</done>
  <what-built>
    Complete embedded terminal: clicking Launch on a worktree opens Claude Code inside Grove in a resizable split-pane terminal. Backend uses portable-pty ConPTY with Tauri Channel streaming. Frontend uses xterm.js with WebGL rendering.
  </what-built>
  <how-to-verify>
    **Dev mode test:**
    1. Run `cargo tauri dev` from the grove directory
    2. Select a project, click "Launch" on any worktree row
    3. Verify: terminal pane opens on the right side of the dashboard
    4. Verify: Claude Code starts loading in the terminal (you see the Claude Code startup output)
    5. Type something and press Enter -- verify keystrokes reach the terminal
    6. Verify: ANSI colors render (Claude Code output should have colors)
    7. Drag the pane divider left/right -- verify both panes resize fluidly, terminal text reflows
    8. Resize the Grove window -- verify terminal adjusts
    9. Close the terminal (X button in toolbar) -- verify terminal pane disappears

    **NAS test (if Z: drive worktree available):**
    10. Select a project on Z: drive, click Launch
    11. Verify: terminal opens without errors (UNC path resolved)

    **Release build test (CRITICAL for NFR-07):**
    12. Run `cargo tauri build` (this takes 10+ minutes on NAS)
    13. Run the built exe from `src-tauri/target/release/Grove.exe`
    14. Launch a terminal session
    15. **CRITICAL CHECK:** Verify NO visible cmd.exe/conhost window flashes during terminal launch
    16. If a window flashes, note the behavior for gap closure

    **What to look for:**
    - NO 1-column terminal collapse (FitAddon working)
    - NO phantom scrollbars
    - NO visible CMD windows (release build only -- dev mode has parent console)
    - Colors and cursor movement work (try arrow keys, clear command)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
- `cargo tauri dev` launches with working embedded terminal
- Full ANSI rendering: colors, cursor movement, clear
- Split pane resizes fluidly with draggable divider
- Terminal works on NAS-hosted worktrees
- Release build: no visible CMD window flash
</verification>

<success_criteria>
- User clicks Launch and Claude Code opens in embedded terminal inside Grove (TERM-01)
- Terminal pane is resizable with branch table on left (TERM-04)
- Full ANSI colors, cursor movement, clearing work (TERM-05)
- Tauri Channels used for I/O, not events (NFR-05)
- UNC paths resolved before PTY spawn (NFR-06)
- No visible CMD windows in release build (NFR-07)
- WebGL rendering with DOM fallback (NFR-08)
</success_criteria>

<output>
After completion, create `.planning/phases/09-terminal-foundation-conpty-spike/09-03-SUMMARY.md`
</output>

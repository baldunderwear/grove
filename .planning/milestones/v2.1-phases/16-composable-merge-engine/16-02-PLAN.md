---
phase: 16-composable-merge-engine
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src-tauri/src/git/pipeline.rs
autonomous: true
requirements: [MERGE-01]

must_haves:
  truths:
    - "MergeContext phase progression follows Init -> Executed -> Bumped -> Changelogged -> Committed"
    - "Calling any step out of order returns GitError::MergeAborted with a message naming the expected phase"
    - "merge_bump() with override_build set uses the override value, not disk detection"
    - "merge_bump() with empty build_patterns is a no-op that advances to Bumped"
    - "merge_changelog() with no changelog_config is a no-op that advances to Changelogged"
    - "into_result() produces success=true only when phase is Committed"
  artifacts:
    - path: "src-tauri/src/git/pipeline.rs"
      provides: "Unit tests in #[cfg(test)] mod tests"
      contains: "mod tests"
  key_links:
    - from: "src-tauri/src/git/pipeline.rs tests"
      to: "src-tauri/src/git/pipeline.rs"
      via: "Tests exercise MergeContext, MergePhase, and step validation"
      pattern: "MergeContext::new|MergePhase::|merge_execute|merge_bump"
---

<objective>
Add unit tests to pipeline.rs verifying phase validation, skip-safe behavior, and override_build support.

Purpose: Ensure the composable merge engine's phase state machine is correct and rejects misuse. These tests are the safety net for Phase 17 when it composes steps in new ways.

Output: Inline `#[cfg(test)]` module in pipeline.rs with tests covering all MERGE-01 sub-requirements.
</objective>

<execution_context>
@Z:/data/development/forge/.claude/get-shit-done/workflows/execute-plan.md
@Z:/data/development/forge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-composable-merge-engine/16-RESEARCH.md
@.planning/phases/16-composable-merge-engine/16-01-SUMMARY.md

@src-tauri/src/git/pipeline.rs
@src-tauri/src/git/error.rs

<interfaces>
<!-- From Plan 01 output -->

From src-tauri/src/git/pipeline.rs (created by Plan 01):
```rust
pub enum MergePhase { Init, Executed, Bumped, Changelogged, Committed }

pub struct MergeContext {
    pub phase: MergePhase,
    pub override_build: Option<u32>,
    pub build_patterns: Vec<BuildFileConfig>,
    pub changelog_config: Option<ChangelogConfig>,
    pub new_build: Option<u32>,
    pub changelog_renames: Vec<(String, String)>,
    // ... other fields
}

impl MergeContext {
    pub fn new(...) -> Self;
    pub fn into_result(self) -> MergeResult;
}

pub fn merge_execute(ctx: &mut MergeContext) -> Result<(), GitError>;
pub fn merge_bump(ctx: &mut MergeContext) -> Result<(), GitError>;
pub fn merge_changelog(ctx: &mut MergeContext) -> Result<(), GitError>;
pub fn merge_commit(ctx: &mut MergeContext) -> Result<(), GitError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit tests for phase validation and skip-safe behavior</name>
  <files>src-tauri/src/git/pipeline.rs</files>
  <behavior>
    - Test: MergeContext::new() initializes with phase=Init and all Option fields as None
    - Test: into_result() returns success=false when phase is not Committed
    - Test: into_result() returns success=true when phase is Committed (manually set phase for unit test)
    - Test: merge_execute() rejects phase != Init (create ctx, manually set phase to Bumped, call merge_execute, expect MergeAborted error)
    - Test: merge_bump() rejects phase != Executed (create ctx at Init, call merge_bump, expect MergeAborted error)
    - Test: merge_changelog() rejects phase != Bumped (create ctx at Init, call merge_changelog, expect MergeAborted error)
    - Test: merge_commit() rejects phase != Changelogged (create ctx at Init, call merge_commit, expect MergeAborted error)
    - Test: merge_bump() with empty build_patterns advances phase to Bumped and sets new_build=None (create ctx, manually set phase to Executed, set build_patterns to empty vec, call merge_bump, assert phase==Bumped and new_build==None)
    - Test: merge_changelog() with changelog_config=None advances phase to Changelogged (create ctx, manually set phase to Bumped, set changelog_config=None, call merge_changelog, assert phase==Changelogged and changelog_renames is empty)
  </behavior>
  <action>
Add a `#[cfg(test)] mod tests` block at the bottom of `pipeline.rs`.

**Phase validation tests** (no git repo needed -- these test the state machine only):

For out-of-order rejection tests, create a MergeContext with `MergeContext::new()` using dummy values (project_path: "/tmp/fake", source_branch: "test-source", merge_target: "main", build_patterns: empty vec, changelog_config: None, override_build: None). Then manually set `ctx.phase` to an incorrect value before calling the step function. Assert the result is `Err` and contains "MergeAborted" or the expected phase name in the error message.

**Skip-safe tests** (no git repo needed -- these test the no-op path):

For merge_bump skip test: Create context, manually set `ctx.phase = MergePhase::Executed` (simulating that execute already ran), leave `build_patterns` empty. Call `merge_bump(&mut ctx)`. Assert `ctx.phase == MergePhase::Bumped` and `ctx.new_build.is_none()`.

For merge_changelog skip test: Create context, manually set `ctx.phase = MergePhase::Bumped`, leave `changelog_config` as None. Call `merge_changelog(&mut ctx)`. Assert `ctx.phase == MergePhase::Changelogged` and `ctx.changelog_renames.is_empty()`.

**into_result tests:**

Create a context, verify `into_result()` returns `success: false` when phase != Committed. Create another, manually set `phase = MergePhase::Committed` and some `commits_merged = 5`, verify `into_result()` returns `success: true` and `commits_merged == 5`.

**Note:** Integration tests requiring a real git repo (merge_execute actually merging, merge_commit creating commits) are NOT included here. Those would require a test fixture with a git repo, branches, and files -- out of scope for this plan. The unit tests here verify the state machine logic which is the new code added by the decomposition.

Use `#[test]` annotations. Follow existing test patterns in build.rs (simple assertions, no external test framework).
  </action>
  <verify>
    <automated>cd Z:/data/development/grove && cargo test -p grove -- pipeline::tests --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>All pipeline unit tests pass. Tests cover: phase initialization, into_result success/failure, out-of-order rejection for all 4 steps, skip-safe behavior for bump and changelog.</done>
</task>

<task type="auto">
  <name>Task 2: Final verification -- clippy clean + cargo test full suite</name>
  <files>src-tauri/src/git/pipeline.rs</files>
  <action>
Run the full test suite and clippy to ensure no regressions:

1. `cargo test -p grove` -- all existing tests (build.rs, changelog.rs, watcher) must still pass
2. `cargo clippy -p grove -- -D warnings` -- no new warnings from pipeline.rs or modified merge.rs
3. `cargo check -p grove` -- clean compilation

If clippy raises warnings in pipeline.rs (unused variables, unnecessary clones, etc.), fix them. Common issues:
- Unused `modified_files` return from `bump_build_number` -- use `let _modified =` if not needed
- Clone vs reference on build_patterns -- check if `to_vec()` is needed in MergeContext::new or if we can take ownership

Do NOT change any logic -- only fix style/lint issues.
  </action>
  <verify>
    <automated>cd Z:/data/development/grove && cargo test -p grove 2>&1 | tail -5 && cargo clippy -p grove -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite green. Clippy clean. No regressions in existing build.rs or changelog.rs tests.</done>
</task>

</tasks>

<verification>
1. `cargo test -p grove -- pipeline::tests` -- all new unit tests pass
2. `cargo test -p grove` -- full suite green (no regressions)
3. `cargo clippy -p grove -- -D warnings` -- clean
4. Pipeline tests cover: phase progression, out-of-order rejection, skip-safe no-ops, into_result
</verification>

<success_criteria>
- At least 7 unit tests in pipeline.rs covering phase state machine
- All tests pass
- Full test suite has no regressions
- Clippy clean
</success_criteria>

<output>
After completion, create `.planning/phases/16-composable-merge-engine/16-02-SUMMARY.md`
</output>

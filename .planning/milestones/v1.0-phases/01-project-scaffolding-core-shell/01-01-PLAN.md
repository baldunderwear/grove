---
phase: 01-project-scaffolding-core-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/capabilities/default.json
  - src-ui/package.json
  - src-ui/tsconfig.json
  - src-ui/vite.config.ts
  - src-ui/index.html
  - src-ui/src/main.tsx
  - src-ui/src/App.tsx
  - src-ui/src/index.css
autonomous: false
requirements:
  - FR-05.1
  - FR-05.4
  - FR-05.5
  - NFR-01.4

must_haves:
  truths:
    - "App compiles and launches with cargo tauri dev"
    - "System tray icon appears when app starts"
    - "Left-clicking tray icon toggles main window visibility"
    - "Right-clicking tray icon shows context menu with Open and Quit"
    - "Closing the window hides it to tray instead of quitting"
    - "App starts with window hidden (minimized to tray)"
    - "Tailwind CSS utility classes render correctly in the UI"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri setup with tray icon, menu, window management"
      contains: "TrayIconBuilder"
    - path: "src-tauri/src/main.rs"
      provides: "Entry point calling lib::run()"
      contains: "grove_lib::run"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri configuration with tray, window, bundler settings"
      contains: "grove-tray"
    - path: "src-ui/src/App.tsx"
      provides: "Root React component with Tailwind styling"
      contains: "Grove"
    - path: "src-ui/vite.config.ts"
      provides: "Vite config with React and Tailwind v4 plugins"
      contains: "@tailwindcss/vite"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-ui/dist"
      via: "frontendDist path"
      pattern: "frontendDist.*src-ui/dist"
    - from: "src-tauri/src/main.rs"
      to: "src-tauri/src/lib.rs"
      via: "grove_lib::run() call"
      pattern: "grove_lib::run"
    - from: "src-tauri/src/lib.rs"
      to: "tauri tray-icon feature"
      via: "TrayIconBuilder in setup()"
      pattern: "TrayIconBuilder"
---

<objective>
Scaffold the complete Tauri 2 + React 19 + TypeScript + Tailwind v4 project and implement the tray-resident app pattern with system tray icon, context menu, and window show/hide.

Purpose: Create the foundational app that all subsequent phases build upon. The tray-resident pattern is Grove's core UX — the app lives in the system tray.
Output: Working Tauri app with tray icon, window toggle, and React frontend with Tailwind.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding-core-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Tauri 2 project with React frontend and Tailwind v4</name>
  <files>
    src-tauri/Cargo.toml
    src-tauri/tauri.conf.json
    src-tauri/src/main.rs
    src-tauri/src/lib.rs
    src-tauri/capabilities/default.json
    src-tauri/build.rs
    src-ui/package.json
    src-ui/tsconfig.json
    src-ui/vite.config.ts
    src-ui/index.html
    src-ui/src/main.tsx
    src-ui/src/App.tsx
    src-ui/src/index.css
    src-ui/src/vite-env.d.ts
  </files>
  <read_first>
    CLAUDE.md
    .planning/phases/01-project-scaffolding-core-shell/01-RESEARCH.md
  </read_first>
  <action>
**Step 0: Ensure Rust toolchain is installed.**
Run `rustup --version`. If not found, install:
```bash
winget install Rustlang.Rustup
```
Then configure:
```bash
rustup default stable-msvc
```
Verify with `rustc --version` and `cargo --version`.

**Step 1: Create src-ui/ frontend manually (NOT via create-tauri-app, to get the src-ui/ directory structure right).**

Create `src-ui/package.json`:
```json
{
  "name": "grove-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "lint": "tsc -b --noEmit"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@tauri-apps/api": "^2.0.0",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "~5.7.0",
    "vite": "^6.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "@tauri-apps/cli": "^2.0.0"
  }
}
```
Note: Use TypeScript 5.7.x (not 6.0) to avoid potential compatibility issues with Vite/React ecosystem per research open question.
Note: Use Vite 6.x (not 8.x) since it's the current stable that Tauri 2 officially supports. The research listed 8.0.3 but Tauri 2 docs reference Vite 6.

Create `src-ui/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src"]
}
```

Create `src-ui/vite.config.ts`:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

const host = process.env.TAURI_DEV_HOST;

export default defineConfig({
  plugins: [react(), tailwindcss()],
  clearScreen: false,
  server: {
    host: host || false,
    port: 5173,
    strictPort: true,
    hmr: host ? { protocol: 'ws', host, port: 5174 } : undefined,
    watch: {
      ignored: ['**/src-tauri/**'],
    },
  },
});
```

Create `src-ui/index.html`:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Grove</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Create `src-ui/src/main.tsx`:
```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

Create `src-ui/src/App.tsx`:
```typescript
function App() {
  return (
    <div className="min-h-screen bg-gray-900 text-white flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-4xl font-bold mb-4">Grove</h1>
        <p className="text-gray-400">Manage your trees.</p>
      </div>
    </div>
  );
}

export default App;
```

Create `src-ui/src/index.css`:
```css
@import "tailwindcss";
```

Create `src-ui/src/vite-env.d.ts`:
```typescript
/// <reference types="vite/client" />
```

Run `cd src-ui && npm install` to install all dependencies.

**Step 2: Create src-tauri/ Rust backend.**

Create `src-tauri/Cargo.toml`:
```toml
[package]
name = "grove"
version = "0.1.0"
edition = "2021"

[lib]
name = "grove_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
```

Create `src-tauri/build.rs`:
```rust
fn main() {
    tauri_build::build()
}
```

Create `src-tauri/src/main.rs`:
```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    grove_lib::run()
}
```

Create `src-tauri/src/lib.rs` with a minimal placeholder (tray setup comes in Task 2):
```rust
pub fn run() {
    tauri::Builder::default()
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

Create `src-tauri/tauri.conf.json`:
```json
{
  "$schema": "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-cli/schema.json",
  "productName": "Grove",
  "version": "0.1.0",
  "identifier": "com.grove.app",
  "build": {
    "frontendDist": "../src-ui/dist",
    "devUrl": "http://localhost:5173",
    "beforeDevCommand": "cd ../src-ui && npm run dev",
    "beforeBuildCommand": "cd ../src-ui && npm run build"
  },
  "app": {
    "withGlobalTauri": false,
    "windows": [
      {
        "label": "main",
        "title": "Grove",
        "width": 1200,
        "height": 800,
        "resizable": true,
        "visible": false
      }
    ]
  },
  "bundle": {
    "active": true,
    "targets": ["nsis", "msi"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.ico",
      "icons/icon.png"
    ],
    "windows": {
      "webviewInstallMode": {
        "type": "downloadBootstrapper"
      }
    }
  }
}
```

Create `src-tauri/capabilities/default.json`:
```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
```

Generate app icons using `cargo tauri icon`. If no source icon exists yet, create a simple placeholder PNG first (a green tree on dark background, or use the Tauri default icons that get generated during `cargo tauri init`). The icons directory `src-tauri/icons/` must contain at minimum: `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.ico`, `icon.png`.

**Step 3: Verify the scaffold compiles and runs.**
Run from project root:
```bash
cd src-tauri && cargo check
```
This verifies Rust compilation without launching. Fix any issues before proceeding.
  </action>
  <verify>
    <automated>cd Z:/data/development/grove/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-ui/package.json contains "react" and "@tauri-apps/api" and "tailwindcss" and "@tailwindcss/vite" and "zustand"
    - src-ui/node_modules/ directory exists (npm install succeeded)
    - src-ui/vite.config.ts contains "import tailwindcss from '@tailwindcss/vite'"
    - src-ui/src/index.css contains '@import "tailwindcss"'
    - src-ui/src/App.tsx contains "min-h-screen" and "Grove" and "Manage your trees"
    - src-tauri/Cargo.toml contains 'name = "grove"' and 'features = ["tray-icon"]'
    - src-tauri/Cargo.toml contains 'name = "grove_lib"' and 'crate-type = ["lib", "cdylib", "staticlib"]'
    - src-tauri/src/main.rs contains "grove_lib::run()"
    - src-tauri/src/lib.rs contains "tauri::Builder::default()"
    - src-tauri/tauri.conf.json contains '"frontendDist": "../src-ui/dist"' and '"devUrl": "http://localhost:5173"'
    - src-tauri/tauri.conf.json contains '"visible": false'
    - src-tauri/tauri.conf.json contains '"downloadBootstrapper"'
    - src-tauri/capabilities/default.json contains '"core:default"'
    - `cargo check` in src-tauri/ exits with code 0
  </acceptance_criteria>
  <done>Tauri 2 project scaffolded with React 19 + Tailwind v4 frontend in src-ui/ and Rust backend in src-tauri/. Rust compiles cleanly. Frontend dependencies installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement system tray with context menu and window show/hide</name>
  <files>
    src-tauri/src/lib.rs
  </files>
  <read_first>
    src-tauri/src/lib.rs
    src-tauri/tauri.conf.json
    .planning/phases/01-project-scaffolding-core-shell/01-RESEARCH.md
  </read_first>
  <action>
Replace the placeholder `src-tauri/src/lib.rs` with the full tray-resident app implementation. This addresses FR-05.1 (tray icon + context menu), FR-05.4 (left-click opens, right-click menus), and FR-05.5 (starts minimized to tray).

Write `src-tauri/src/lib.rs` with this exact content:

```rust
use tauri::{
    menu::{Menu, MenuItem, PredefinedMenuItem},
    tray::{MouseButton, MouseButtonState, TrayIconBuilder, TrayIconEvent},
    Manager,
};

pub fn run() {
    tauri::Builder::default()
        .setup(|app| {
            // Build tray context menu
            let quit = MenuItem::with_id(app, "quit", "Quit Grove", true, None::<&str>)?;
            let show = MenuItem::with_id(app, "show", "Open Grove", true, None::<&str>)?;
            let separator = PredefinedMenuItem::separator(app)?;
            let menu = Menu::with_items(app, &[&show, &separator, &quit])?;

            // Build tray icon
            let _tray = TrayIconBuilder::new()
                .id("grove-tray")
                .icon(app.default_window_icon().unwrap().clone())
                .tooltip("Grove - Manage your trees")
                .menu(&menu)
                .menu_on_left_click(false)
                .on_menu_event(|app, event| match event.id.as_ref() {
                    "quit" => app.exit(0),
                    "show" => {
                        if let Some(window) = app.get_webview_window("main") {
                            let _ = window.unminimize();
                            let _ = window.show();
                            let _ = window.set_focus();
                        }
                    }
                    _ => {}
                })
                .on_tray_icon_event(|tray, event| {
                    // Left-click toggles window visibility (FR-05.4)
                    if let TrayIconEvent::Click {
                        button: MouseButton::Left,
                        button_state: MouseButtonState::Up,
                        ..
                    } = event
                    {
                        let app = tray.app_handle();
                        if let Some(window) = app.get_webview_window("main") {
                            if window.is_visible().unwrap_or(false) {
                                let _ = window.hide();
                            } else {
                                let _ = window.unminimize();
                                let _ = window.show();
                                let _ = window.set_focus();
                            }
                        }
                    }
                })
                .build(app)?;

            // Intercept window close to hide instead of quit (tray-resident pattern)
            if let Some(window) = app.get_webview_window("main") {
                let w = window.clone();
                window.on_window_event(move |event| {
                    if let tauri::WindowEvent::CloseRequested { api, .. } = event {
                        api.prevent_close();
                        let _ = w.hide();
                    }
                });
            }

            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

After writing, verify compilation:
```bash
cd src-tauri && cargo check
```
  </action>
  <verify>
    <automated>cd Z:/data/development/grove/src-tauri && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/lib.rs contains "TrayIconBuilder::new()"
    - src-tauri/src/lib.rs contains '.id("grove-tray")'
    - src-tauri/src/lib.rs contains '.tooltip("Grove - Manage your trees")'
    - src-tauri/src/lib.rs contains "menu_on_left_click(false)"
    - src-tauri/src/lib.rs contains 'MenuItem::with_id(app, "quit", "Quit Grove"'
    - src-tauri/src/lib.rs contains 'MenuItem::with_id(app, "show", "Open Grove"'
    - src-tauri/src/lib.rs contains "MouseButton::Left"
    - src-tauri/src/lib.rs contains "api.prevent_close()"
    - src-tauri/src/lib.rs contains "window.hide()"
    - src-tauri/src/lib.rs contains "window.show()"
    - `cargo check` in src-tauri/ exits with code 0
  </acceptance_criteria>
  <done>System tray icon appears with "Open Grove" and "Quit Grove" menu items. Left-click toggles window. Right-click shows menu. Window close hides to tray. App starts with window hidden.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify tray-resident app works</name>
  <files>src-tauri/src/lib.rs</files>
  <action>
Launch the app with `cd src-tauri && cargo tauri dev` and have the user verify:
1. System tray icon appears in Windows taskbar notification area
2. Left-click tray icon: main window appears with "Grove" heading and "Manage your trees." on dark background
3. Left-click tray icon again: window hides
4. Right-click tray icon: context menu with "Open Grove" and "Quit Grove"
5. Click "Open Grove": window appears
6. Click window X button: window hides (does NOT quit -- tray icon remains)
7. Right-click tray icon, click "Quit Grove": app exits completely
8. Tailwind working: heading is large/bold, white text on dark gray background
  </action>
  <verify>
    <automated>cd Z:/data/development/grove/src-tauri && cargo check 2>&1 | tail -3</automated>
  </verify>
  <done>User confirmed: tray icon visible, left-click toggles window, right-click shows menu, close hides to tray, Quit exits, Tailwind renders correctly.</done>
</task>

</tasks>

<verification>
- `cargo check` passes in src-tauri/ (Rust compiles)
- `npm run build` passes in src-ui/ (frontend builds)
- App launches with `cargo tauri dev`
- Tray icon visible in system tray
- Window toggles on left-click
- Context menu appears on right-click
- Window close hides to tray (does not quit)
- Tailwind classes render correctly
</verification>

<success_criteria>
Working Tauri 2 tray-resident application: tray icon with Open/Quit menu, left-click toggle, close-to-tray, React 19 + Tailwind v4 frontend displaying "Grove" branding. Rust compiles, frontend builds, app launches successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-core-shell/01-01-SUMMARY.md`
</output>

# node_modules cannot be created on the NAS (Z: drive blocks the directory name).
# All npm operations for this project must use the local C: drive mirror.
# See scripts/npm-local.sh for the workaround.
